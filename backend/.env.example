# ============================================
# GCP Invoice Platform - Backend Configuration
# ============================================

# -----------------
# GCP Configuration
# -----------------
# Your Google Cloud Project ID (found in GCP Console)
GCP_PROJECT_ID=your-project-id

# GCP region where services are deployed (recommended: us-central1)
GCP_REGION=us-central1

# -----------------------
# Firebase Configuration
# -----------------------
# Path to Firebase Admin SDK service account JSON file (for local development)
# Download from: Firebase Console > Project Settings > Service Accounts
FIREBASE_SERVICE_ACCOUNT_PATH=path/to/serviceAccountKey.json

# OR use Secret Manager for production (recommended for Cloud Run)
# Format: projects/PROJECT_ID/secrets/SECRET_NAME/versions/VERSION
FIREBASE_SERVICE_ACCOUNT_SECRET=projects/PROJECT_ID/secrets/firebase-key/versions/latest

# -------------------
# Cloud Storage
# -------------------
# Cloud Storage bucket name for storing PDF invoices
# Format: {project-id}-invoices
# Must be globally unique and created before deployment
STORAGE_BUCKET_NAME=your-project-id-invoices

# ---------
# Firestore
# ---------
# Firestore database name (use "(default)" for the default database)
FIRESTORE_DATABASE=(default)

# ---------------------------
# Application Configuration
# ---------------------------
# Environment: development, staging, or production
ENVIRONMENT=development

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Allowed CORS origins (comma-separated)
# For local dev: http://localhost:5173,http://localhost:3000
# For production: https://your-domain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ---------------------
# File Upload Limits
# ---------------------
# Maximum file size in megabytes (default: 10MB)
MAX_FILE_SIZE_MB=10

# Allowed MIME types for uploads (comma-separated)
ALLOWED_FILE_TYPES=application/pdf

# ----------
# Security
# ----------
# Signed URL expiration time in hours (for secure PDF downloads)
SIGNED_URL_EXPIRATION_HOURS=1

# Rate limiting (requests per minute per user)
RATE_LIMIT_PER_MINUTE=100
